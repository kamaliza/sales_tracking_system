{
  "name": "sales-backend",
  "version": "1.0.2",
  "description": "Backend for Sales Tracking System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "test:coverage": "jest --coverage --watchAll=false",
    "lint": "eslint . --ext .js || echo 'ESLint not configured'",
    "lint:fix": "eslint . --ext .js --fix || echo 'ESLint not configured'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "prom-client": "^15.1.0"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "supertest": "^6.3.3"
  }
}
