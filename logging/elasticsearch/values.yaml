# Elasticsearch configuration for ELK stack
# This is a simplified configuration file
# For production, use the official Elasticsearch Helm chart

replicas: 1
resources:
  requests:
    cpu: 1000m
    memory: 2Gi
  limits:
    cpu: 2000m
    memory: 4Gi
storage:
  size: 50Gi
  storageClass: standard

